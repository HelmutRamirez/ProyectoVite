config:
  target: "http://localhost:3001"
  phases:
    - arrivalCount: 10000   # crea exactamente 10 000 usuarios en total
      duration: 60          # los reparte en 60 segundos
scenarios:
  - flow:
    - post:
        url: "/registro"
        json:
          nombre: "UsuarioTest"
          correo: "test@example.com"
          contraseña: "123456"


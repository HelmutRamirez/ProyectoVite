config:
  target: "http://localhost:3001" #api o url que se el enviaran las solicitudes
  phases:
    - arrivalCount: 10000   # son la cantidadd de soliciutude que se encian a la api
      duration: 60          # tiempo en ql que se envian las consultas, osea las divide en los 60 segundos
scenarios:
  - flow:
    - post:
        url: "/registro"
        json:
          nombre: "UsuarioTest"
          correo: "test@example.com"
          contraseña: "123456"

